apiVersion: v1
kind: Service
metadata:
  name: simple-api
  namespace: study
spec:
  selector:
    app: simple-api-app
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: LoadBalancer
